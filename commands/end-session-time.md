---
description: Log the end of the current session in time-anchor
---

Use the time-anchor skill to close the current session.

Run `end_session.py` and report the session duration to the user (started at X, ended at Y, duration Z).
